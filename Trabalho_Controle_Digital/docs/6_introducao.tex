\chapter{Introdução}


\section{Introdução e Revisão bibliográfica}

Modelo de como inserir uma figura, (Fig. \ref{fig:micro_2}).

\begin{figure}[htp]
	\centering
	\includegraphics[width=1\textwidth]{arquivos/micro_2}
	\caption{Modelo para imagens. \textbf{A} and \textbf{C}: SCB samples without the use of pre-treatments. \textbf{B} and \textbf{D}: seSCB samples. The arrows evidences areas of greater disarrangement of the structure of the seSCB in comparison to the SCB}
	\label{fig:micro_2}
\end{figure}


Modelo de como inserir uma Tabela, (Tab. \ref{tab:tab1}). Uma opção para criação de tabelas é o site
\href{https://www.latex-tables.com/}{www.latex-tables.com}.

\begin{table}[htp]
\label{tab:tab1}
\centering
\caption{Tabela exemplo}
\begin{tabular}{lll} 
\hline
\textbf{item 1} & \textbf{item 2} & \textbf{item 3}   \\ 

\hline
valor 1         & valor 1         & valor 1  *       \\
valor 2         & valor 2 valor 1         & valor 2    \\ 
\hline         
\end{tabular}


		\footnotesize * modelo de nota de rodapé para tabela \\
\end{table}

\subsection{Subtítulo}
Texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto.
\subsubsection{Sub subtítulo}

Texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto, texto.
